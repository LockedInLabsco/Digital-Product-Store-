/**
 * Shared SlowDay copy for the two surfaces that show this waitlist —
 * the homepage's hardcoded section (AppWaitlistSection) and the
 * dedicated /waitlist/[slug] page. Kept in one place so both stay in
 * sync instead of duplicating the same strings.
 */

export const SLOWDAY_WAITLIST_SLUG = 'phone-control-app'

export const SLOWDAY_EYEBROW = 'SlowDay'

export const SLOWDAY_HEADLINE = 'A calmer way to take back control of your phone.'

export const SLOWDAY_SUPPORTING_TEXT =
  'SlowDay is a minimal app for reducing screen addiction through screen-time awareness, app control, grayscale, focus, and better digital habits. Join the waitlist to get early access and follow the build.'

export const SLOWDAY_BUTTON_TEXT = 'Join the waitlist'

export const SLOWDAY_FEATURES = [
  'Screen time insights',
  'App blocking',
  'Grayscale controls',
  'Focus sessions',
  'Reward-based access',
]
